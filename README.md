# Cara Berkontribusi
1. Fork repo ini
2. Buat branch baru: `git checkout -b fitur-baru`
3. Commit perubahan: `git commit -m "Tambah fitur baru"`
4. Push ke fork kamu: `git push origin fitur-baru`
5. Buat pull request ke branch `main`
